{
  "devDependencies": {
    "@babel/cli": "^7.16.8",
    "@babel/core": "^7.16.12",
    "tailwindcss": "^3.0.15"
  },
  "scripts": {
    "build:css": "tailwindcss build -i src/input.css -o dist/output.css --watch",
    "build:babel": "babel src -d dist"
  }
}
